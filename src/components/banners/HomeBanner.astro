<section
  class="w-screen min-h-[80vh] p-5 flex flex-col md:flex-row items-center justify-between overflow-hidden relative bg-[#151c28]"
>
  <!-- Video Background -->
  <video
    autoplay
    muted
    loop
    playsinline
    class="absolute inset-0 w-full h-full object-cover z-0"
    style="filter: blur(12px) brightness(0.5);"
  >
    <source src="https://videos.pexels.com/video-files/7649289/7649289-uhd_2560_1440_30fps.mp4" type="video/mp4" />
  </video>

  <!-- Overlay for extra blur and contrast -->
  <div class="absolute inset-0 bg-[#151c28]/20 backdrop-blur-sm z-0"></div>
  <div
    class="relative z-20 w-full flex flex-col md:flex-row items-center justify-between"
  >
    <div
      class="flex-1 flex flex-col items-start justify-center px-6 md:px-0 max-w-7xl mx-auto"
    >
      <h1
        class="text-4xl md:text-5xl font-extrabold leading-tight mb-4 text-white flex items-center gap-3"
        style="letter-spacing: -0.01em; filter: blur(0.5px);"
      >
        The <span class="text-blue-400 font-bold italic">fastest</span> way
        <br class="hidden md:block" />
        to automate your business
      </h1>
      <ul class="mb-6 space-y-2 text-base md:text-lg" style="color: #e0e7ef; filter: blur(0.5px);">
        <li class="flex items-center gap-2">
          <i class="fa-solid fa-circle-check text-blue-400"></i>
          <span
            ><b>All-in-one platform.</b> Manage, automate, and monitor all your bots
            and agents in one place.</span
          >
        </li>
        <li class="flex items-center gap-2">
          <i class="fa-solid fa-server text-blue-400"></i>
          <span
            ><b>Agentic AI & MCP Servers.</b> Next-gen automation with secure, scalable
            infrastructure.</span
          >
        </li>
        <li class="flex items-center gap-2">
          <i class="fa-solid fa-comments text-blue-300"></i>
          <span>Chat with your bots and agents in real time.</span>
        </li>
      </ul>
      <div class="mb-6" style="filter: blur(0.5px);">
        <span class="block text-lg font-semibold mb-2 text-white"
          >I want to:</span
        >
        <div class="flex flex-col md:flex-row gap-4">
          <button
            class="flex-1 px-6 py-4 rounded-2xl border-2 border-blue-800 bg-[#232b3a] text-blue-200 font-semibold shadow hover:border-blue-400 hover:bg-blue-900 transition focus:outline-none focus:ring-2 focus:ring-blue-300 cursor-pointer flex items-center justify-center gap-2"
            aria-label="Create Agent"
          >
            <i class="fa-solid fa-user-robot"></i> Create Agent
          </button>
          <button
            class="flex-1 px-6 py-4 rounded-2xl border-2 border-blue-800 bg-[#232b3a] text-blue-200 font-semibold shadow hover:border-blue-400 hover:bg-blue-900 transition focus:outline-none focus:ring-2 focus:ring-blue-300 cursor-pointer flex items-center justify-center gap-2"
            aria-label="Create MCP Server"
          >
            <i class="fa-solid fa-network-wired"></i> Create MCP Server
          </button>
          <button
            class="flex-1 px-6 py-4 rounded-2xl border-2 border-blue-800 bg-[#232b3a] text-blue-200 font-semibold shadow hover:border-blue-400 hover:bg-blue-900 transition focus:outline-none focus:ring-2 focus:ring-blue-300 cursor-pointer flex items-center justify-center gap-2"
            aria-label="Create Bot"
          >
            <i class="fa-solid fa-robot"></i> Create Bot
          </button>
        </div>
      </div>
      <form
        class="flex flex-col md:flex-row items-center gap-4 w-full max-w-lg mb-2"
        autocomplete="on"
        style="filter: blur(0.5px);"
      >
        <label class="sr-only">Email</label>
        <input
          id="signup-input"
          type="email"
          placeholder="Enter your email"
          class="flex-1 px-5 py-3 rounded-2xl border border-blue-800 bg-[#232b3a] text-white text-base shadow focus:ring-2 focus:ring-blue-300 focus:outline-none"
          required
          aria-label="Enter your email"
        />
        <button
          type="submit"
          class="px-8 py-3 rounded-2xl bg-blue-600 text-white font-bold shadow hover:bg-blue-700 transition cursor-pointer"
          aria-label="Get Started"
        >
          Get Started
        </button>
      </form>
      <div
        class="flex items-center gap-2 mt-2 text-blue-200"
        style="filter: blur(0.5px);"
      >
        <span>Or get started with</span>
        <button
          class="rounded-full bg-[#232b3a] border border-blue-800 shadow p-2 hover:bg-blue-900 transition cursor-pointer"
          aria-label="Sign in with Google"
        >
          <i class="fa-brands fa-google text-lg"></i>
        </button>
      </div>
    </div>
  </div>
</section>
